# ChaCrab Database Configuration
# Default: SQLite (single file, zero setup)
DATABASE_URL=sqlite://chacrab.db

# Alternative: PostgreSQL (for power users)
# DATABASE_URL=postgres://username:password@localhost/chacrab
